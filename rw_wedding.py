import streamlit as st

# 設定網頁標題與分頁圖示
st.set_page_config(page_title="弘智 & 凡宸 婚禮邀請", page_icon="💍")

# --- 1. 新人資訊與圖片 ---
st.title("❤️ 弘智 & 凡宸 的婚禮派對")

# ======= 在這裡加入音樂播放器 =======
st.write("🎵 開啟音樂更浪漫喔～")
# 加入 start_time=0 確保從頭播，autoplay=True 開啟自動播放
st.audio("How_Long_Will_I_Love_You.mp3", format="audio/mp3", start_time=0, autoplay=True)
# =================================

st.write("我們結婚了！歡迎大家來參加我們的婚宴。")

# 建議將你的 Google Drive 圖片設為公開後，使用其直連連結，或是放在 GitHub 上
# 這裡先預留兩個圖片位置
col1, col2 = st.columns(2)
with col1:
    st.image("photo1.jpg", use_container_width=True)
with col2:
    st.image("photo2.jpg", use_container_width=True)

# --- 2. 婚禮資訊 (根據圖片內容更新) ---
st.header("📅 宴客資訊")
st.success("""
- **日期**：2026 年 1 月 31 日 (星期六) 
- **時間**：12:00 宴客入席 / 12:30 午宴開席 
- **地點**：香格里拉冬山河渡假飯店 
- **地址**：宜蘭縣五結鄉公園二路 15 號 
""")

# --- 3. 座位查詢功能 ---
st.header("🔍 查詢我的座位")

# 這裡建議建立一個座位清單（您可以自行擴充）
seat_data = {
    "中和周湯豪": "主桌",
    "可愛莎莎姊": "也是主桌",
    "任弘智": "主桌啦",
    "王凡宸": "主桌主桌"
}

user_input = st.text_input("請輸入您的姓名或綽號 (例如：周天成)")

if user_input:
    # 去除空白後比對
    result = seat_data.get(user_input.strip())
    
    if result:
        st.balloons() # 查詢成功的小特效
        st.success(f"🎊 嗨 {user_input}！您的座位在：【{result}】")
    else:
        st.warning("查無資料，請確認名字是否輸入正確（要跟喜帖名單一樣喔！）。")

# --- 4. Google 地圖導航 ---
st.markdown("---")
st.write("📍 [點我打開 Google 地圖導航 (香格里拉冬山河渡假飯店)](https://www.google.com/maps/search/?api=1&query=宜蘭縣五結鄉公園二路15號)")
